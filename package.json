{
  "name": "enduro-challenge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter web dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "deploy": "pnpm --filter infra deploy",
    "deploy:prod": "pnpm --filter infra deploy -- --context env=prod"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
